import React from 'react';
import { IProps } from './CircularProgress.types';
import { CircularProgressStyled } from './CircularProgress.style';

const CircularProgress = (props: IProps) => <CircularProgressStyled {...props} />;

export default CircularProgress;

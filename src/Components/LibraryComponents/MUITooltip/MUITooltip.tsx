import React from 'react';
import { TooltipStyled } from './MUITooltip.styles';
import { IProps } from './MUITooltip.types';

const MUITooltip = (props: IProps) => <TooltipStyled {...props} />;

export default MUITooltip;
